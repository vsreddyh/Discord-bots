# Food bot (Caped Baldy / Saitama) — secrets, git-ignored.
HERMES_API_KEY=zen-proxy
DISCORD_BOT_TOKEN=
DISCORD_HOME_CHANNEL=
DISCORD_ALLOW_ALL_USERS=true
USDA_API_KEY=DEMO_KEY

# Remote MongoDB (health data; date rows pruned monthly, weight never touched).
# MONGODB_URI=mongodb+srv://user:pass@cluster0.example.mongodb.net/
# MONGODB_DB=hermes
