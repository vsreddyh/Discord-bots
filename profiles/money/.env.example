# Money bot (Miser) — secrets, git-ignored.
HERMES_API_KEY=zen-proxy
DISCORD_BOT_TOKEN=
DISCORD_HOME_CHANNEL=
DISCORD_ALLOW_ALL_USERS=true

# Remote MongoDB (transactions collection; autowiped every ~3 months).
# MONGODB_URI=mongodb+srv://user:pass@cluster0.example.mongodb.net/
# MONGODB_DB=hermes
